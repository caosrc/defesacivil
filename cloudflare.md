# Cloudflare Pages

1. Rode `npm run build`.
2. Publique a pasta `dist`.
3. Este projeto precisa de uma API separada para `/api` e `/ws` se você quiser manter tudo funcionando no Cloudflare.
4. Se quiser só o frontend, ele publica normalmente como SPA estática.
